<div id="navigation">
  <nav class="navbar navbar-expand-lg navbar-dark px-2">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        Rent <span class="highlight">&</span> Ride
      </a>

      <!-- Collapse Button for small screens -->
      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active text-center" aria-current="page" href="/"
              >Home</a
            >
          </li>
          <li class="nav-item">
            <a class="nav-link text-center" href="#">About</a>
          </li>
          <li class="nav-item">
            <a
              class="nav-link text-center"
              href="mailto:rathoredvd3001@email.com"
              >Contact</a
            >
          </li>
          <% if(session.role == 'rider') { %>
          <li class="nav-item">
            <a class="nav-link text-center" href="/rider-profile"
              >Rider Profile</a
            >
          </li>
          <li class="nav-item">
            <a class="nav-link text-center" href="/rider-profile/all-orders"
              >All Rides</a
            >
          </li>

          <% } else if(session.role == 'admin'){ %>
          <li class="nav-item">
            <a class="nav-link text-center" href="/dashboard">Dashboard</a>
          </li>
          <% } else if(session.role == 'renter') { %>
          <li class="nav-item">
            <a class="nav-link text-center" href="/renter-profile"
              >Renter Profile</a
            >
          </li>
          <% } %> <% if(session.role) { %>
          <li class="nav-item text-center">
            <form action="/logout" method="post">
              <button class="logout" type="submit">Logout</button>
            </form>
          </li>
          <% } else { %>
          <li class="nav-item">
            <a class="nav-link text-center" href="/login">Login</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-center" href="/register/rider">Register</a>
          </li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>
</div>
